import { useState } from "react";
import {
  Box,
  TextField,
  Button,
  Typography,
  InputBase,
  Paper,
  Stack,
  Card,
  CardContent,
  Grid,
} from "@mui/material";
import Chat from "./Chat";

const Borrow = () => {
  return <Chat context="borrow" />;
};

export default Borrow;
